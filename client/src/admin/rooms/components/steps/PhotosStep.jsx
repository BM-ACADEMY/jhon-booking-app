import GallerySection from '../sections/GallerySection';

/** Wizard step 5 — photo upload / labels / replace. */
const PhotosStep = (props) => <GallerySection {...props} />;

export default PhotosStep;
