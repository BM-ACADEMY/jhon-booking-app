import GeneralSection from '../sections/GeneralSection';

/** Wizard step 1 — general details plus base price / price unit. */
const GeneralStep = (props) => <GeneralSection {...props} showPricing />;

export default GeneralStep;
