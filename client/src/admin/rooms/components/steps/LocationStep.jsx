import { LocationFields } from '../sections/AdvancedSection';

/** Wizard step 4 — address + Google Map link (incl. goo.gl resolve). */
const LocationStep = (props) => <LocationFields {...props} />;

export default LocationStep;
